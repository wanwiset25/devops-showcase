helm install showcase-dev showcase-chart/ -f showcase-chart/values-dev.yaml  
helm upgrade showcase-dev showcase-chart/ -f showcase-chart/values-dev.yaml  

aws eks update-kubeconfig --region ap-northeast-2 --name showcase-cluster 

kubectl -n argocd port-forward svc/argocd-server 2222:80

kubectl get secrets argocd-initial-admin-secret -o yaml -n argocd
echo "SECRET_ABOVE_STEP" | base64 -d



1. apply terraform (will argocd error)
2. bind kubectl with new cluster
3. apply terraform (will success argocd)
4. port-forward kubectl cluster -n argocd 8080 to local machine
5. get secrets initial argocd password and decode base64